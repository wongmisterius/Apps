# PyTorch con CUDA se instala APARTE, antes que este archivo (ver README) --
# la version exacta depende de tu driver NVIDIA, por eso no se pinea aca.

# Upscale / restauración de video (GPU)
realesrgan==0.3.0
basicsr==1.4.2
gfpgan==1.3.8          # restauración de rostros, opcional
ffmpeg-python==0.2.0

# Interpolación de fotogramas (suavizado a 60fps), opcional
rife-ncnn-vulkan-python==1.4.0 ; platform_system == "Windows"

# Remasterización de audio (denoise + realce de voz, GPU)
deepfilternet==0.5.6
soundfile==0.12.1
numpy<2.0

# GUI de escritorio
PySide6==6.7.2

tqdm==4.66.5
